# 초기 리눅스 환경 세팅 방법
```
wsl --install

# 특정 버전의 Ubuntu 설치
wsl --list --online
wsl --install -d Ubuntu(원하는 버전)
wsl --install -d Ubuntu-24.04

# 설치 후 패키지 업그레이드
wsl

sudo apt update 
sudo apt upgrade
```
